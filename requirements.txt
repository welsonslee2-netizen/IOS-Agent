# iOS Agent 依赖列表

# ── 核心框架 ──────────────────────────────────
# BeeWare UI 框架（iOS 原生 UI 封装）
toga>=0.4.5

# ── 网络 ──────────────────────────────────────
# 异步 HTTP 客户端（调用 LLM API）
httpx>=0.27.0

# ── 开发工具（仅本地，不打包进 App）──────────
briefcase>=0.3.19       # iOS App 构建工具

# ── 代码质量 ──────────────────────────────────
ruff>=0.4.0             # 代码检查
